form(action="/?redirectTo=/"+parentId method="post")
  input(type="hidden" name="parentId" value=parentId placeholder="parentId")
  textarea(type="text" name="content" placeholder="content")
  button(type="submit") submit